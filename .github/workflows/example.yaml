name: example

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Set up
    runs-on: ubuntu-latest
    steps:
      - name: "jhon doe"
        uses: actions/checkout@v2
        run: echo "hello"
